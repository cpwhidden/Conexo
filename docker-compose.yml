services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: conexo
      POSTGRES_PASSWORD: conexo
      POSTGRES_DB: conexo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
